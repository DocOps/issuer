source 'https://rubygems.org'

# Specify your gem's dependencies in issuer.gemspec
gemspec

# Development dependencies
# Note: rubocop and rubocop-rspec are provided by docopslab-dev
group :development, :test do
  gem 'rspec', '~> 3.0'
end

group :development do
  gem 'rake', '~> 13.0'
  gem 'asciidoctor', '~> 2.0'
  gem 'docopslab-dev', '~> 0.1.0'
  gem 'releasehx', '~> 0.1.2'
end
