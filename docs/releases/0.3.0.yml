code: "0.3.0"
date: 2026-01-23
memo:
changes:

  - chid: 0_3_0-35
    tick: 35
    part: cli
    summ: Added a `--json [PATH]` option for writing locally instead of to remote API
    note: |
      Added  CLI option to save GitHub API issue payloads as JSON files locally instead of posting to GitHub, enabling payload inspection and workflow automation.
    lead: briandominick

  - chid: 0_3_0-34
    tick: 34
    part: cli
    summ: Made `issuer help` commandline return help screen instead of error
    note: |
      Running the command with `help` as the first/only argument returns `Error: File not found: help` when it should return the help/usage screen.